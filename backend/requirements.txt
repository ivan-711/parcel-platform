# Core framework
fastapi~=0.134.0
uvicorn[standard]~=0.41.0
python-multipart~=0.0.22

# Database
sqlalchemy~=2.0.47
alembic~=1.18.4
psycopg2-binary>=2.9.0  # TODO: pin after verifying production version
pgvector>=0.3.0  # TODO: pin after verifying production version

# Config & validation
python-dotenv~=1.2.1
pydantic~=2.12.5
pydantic-settings~=2.13.1

# Auth
python-jose[cryptography]~=3.5.0
clerk-backend-api>=1.0.0  # TODO: pin after verifying production version

# AI
anthropic~=0.84.0
openai~=2.30.0

# HTTP client (transitive but directly imported in backend services)
httpx~=0.28.1

# File processing
pdfplumber~=0.11.9
python-docx~=1.2.0

# Cloud storage
boto3~=1.42.59

# Email
resend~=2.23.0

# Rate limiting
slowapi~=0.1.9

# Billing
stripe~=15.0.0

# Observability
sentry-sdk[fastapi]>=2.0.0  # TODO: pin after verifying production version
posthog>=3.0.0  # TODO: pin after verifying production version

# Background jobs
playwright>=1.49.0  # TODO: pin after verifying production version
dramatiq[redis]>=1.16.0  # TODO: pin after verifying production version
