import { AppShell } from '@/components/layout/AppShell'

/** Deal results page — KPI cards, risk score output. Coming in Phase 2. */
export default function DealResults() {
  return (
    <AppShell title="Deal Results">
      <div className="flex items-center justify-center h-64">
        <p className="text-text-muted text-sm font-mono">Coming in Phase 2</p>
      </div>
    </AppShell>
  )
}
