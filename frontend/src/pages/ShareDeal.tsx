/** Public read-only deal share page — no AppShell. Coming in Phase 3. */
export default function ShareDeal() {
  return (
    <div className="min-h-screen bg-app-bg flex items-center justify-center">
      <p className="text-text-muted text-sm font-mono">Coming in Phase 3</p>
    </div>
  )
}
